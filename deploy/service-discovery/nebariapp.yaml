apiVersion: reconcilers.nebari.dev/v1
kind: NebariApp
metadata:
  name: service-discovery
  namespace: nebari-system
spec:
  hostname: service-discovery.nebari.example.com
  service:
    name: service-discovery
    port: 8080
  routing:
    tls:
      enabled: true
  # Landing page itself is publicly accessible
  # Frontend handles authentication via Keycloak client
  auth:
    enabled: false
  landingPage:
    enabled: true
    displayName: "Nebari Portal"
    description: "Central hub for all Nebari services"
    icon: "dashboard"
    category: "Platform"
    priority: 1
    visibility: "public"
