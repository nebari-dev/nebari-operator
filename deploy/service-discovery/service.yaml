apiVersion: v1
kind: Service
metadata:
  name: service-discovery
  namespace: nebari-system
  labels:
    app: service-discovery
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: service-discovery
