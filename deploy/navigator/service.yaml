apiVersion: v1
kind: Service
metadata:
  name: navigator
  namespace: nebari-system
  labels:
    app: navigator
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: navigator
