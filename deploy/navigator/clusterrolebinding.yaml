apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: nebari-navigator-reader
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: nebari-navigator-reader
subjects:
  - kind: ServiceAccount
    name: navigator
    namespace: nebari-system
