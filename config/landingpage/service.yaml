apiVersion: v1
kind: Service
metadata:
  name: landing-page
  namespace: nebari-system
  labels:
    app: landing-page
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: landing-page
