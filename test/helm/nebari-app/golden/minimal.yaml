---
# Source: fixture/templates/minimal.yaml
apiVersion: reconcilers.nebari.dev/v1
kind: NebariApp
metadata:
  name: metatada.name
spec:
  hostname: spec.hostname
  service:
    name: spec.service.name
    port: 0
