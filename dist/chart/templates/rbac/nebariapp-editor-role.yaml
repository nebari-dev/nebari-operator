{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: nic-operator
    name: nic-operator-nebariapp-editor-role
rules:
    - apiGroups:
        - reconcilers.nebari.dev
      resources:
        - nebariapps
      verbs:
        - create
        - delete
        - get
        - list
        - patch
        - update
        - watch
    - apiGroups:
        - reconcilers.nebari.dev
      resources:
        - nebariapps/status
      verbs:
        - get
{{- end }}
